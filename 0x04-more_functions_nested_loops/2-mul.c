#include "main.h"

/**
 * mul - multiplies two integers
 * @a: input character for function
 * @b: input character for function
 * Return: 1 if c is digit, 0 if otherwise
 */

int mul(int a, int b)
{
	int res;

	res = a * b;

	return (res);
}
